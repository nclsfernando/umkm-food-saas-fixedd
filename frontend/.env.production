# Same-origin API on Vercel (Next.js Route Handlers under /api/v1).
# If Vercel Project Env still has an absolute Railway/Zeabur URL, delete or change
# Production NEXT_PUBLIC_API_URL to /api/v1 — code also ignores absolute remote hosts.
NEXT_PUBLIC_API_URL=/api/v1
